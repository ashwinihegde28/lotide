// Reverse the input given from the console and reverse it without using Array.prototype.reverse or Array.prototype.join functions.
const args = process.argv;
const arg = args.slice(2);
for (let i = arg.length;i>0;i--){
let newArr = 
}